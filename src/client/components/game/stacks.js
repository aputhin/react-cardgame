import './stacks.scss'

import React from 'react'
import Card from './card'

export default function Stacks(props) {
  const {stacks, opSelectStack, selectStack, ourStackId} = props
  return <p>STACKS STUB</p>
}