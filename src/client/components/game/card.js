import './card.scss'

import React from 'react'

export default function Card(props) {
  const {isSelectable = false, onClick, type, card, style = 'default', canZoom = false} = props

  return <p>CARD STUB</p>
}